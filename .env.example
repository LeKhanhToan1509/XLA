# Optimized training config
# Đặt file này ở root: .env

NUM_CLASSES=13
BATCH_SIZE=64
LEARNING_RATE=0.001
GPU_AVAILABLE=True
ROOT_DIR=./data

# Tips for faster training:
# - BATCH_SIZE=64 or 128 (GPU works better with larger batches)
# - Use smaller subset for testing first
# - Consider using mixed precision if available
